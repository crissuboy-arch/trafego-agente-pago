---
description: "Plano de teste de criativos: matriz de ângulos/hooks, desenho do teste e critério de vencedor"
argument-hint: "[produto, criativos atuais, métricas]"
---

Use a skill `trafego-agente-pago:trafego-pago` como orquestradora e execute o fluxo **TESTAR CRIATIVOS**.

Pedido do usuário: $ARGUMENTS

Etapas: creative-strategy (matriz de ângulos, hooks e formatos) → optimization (desenho do teste, orçamento, critério de vencedor) → ad-copy (textos das variações, se necessário).

Regras: carregue cada skill especializada só ao chegar na etapa dela; pergunte apenas o que for bloqueante (máx. 3 perguntas numa mensagem); declare suposições; não publique, não gaste e não altere orçamento em conta real sem confirmação explícita.
